models:
  - name: dim__weather_units
    config: &dim_common_config
      contract:
        enforced: true
    columns: &dim_common_columns
      - name: field
        data_type: varchar
        constraints:
          - type: primary_key
            warn_unenforced: false
      - name: units
        data_type: varchar

  - name: dim__air_quality_units
    config: *dim_common_config
    columns: *dim_common_columns

  - name: dim__satellite_radiation_units
    config: *dim_common_config
    columns: *dim_common_columns

  - name: dim__flood_units
    config: *dim_common_config
    columns: *dim_common_columns

  - name: dim__locations
    config: *dim_common_config
    columns:
      - name: location_id
        data_type: number
        constraints:
          - type: primary_key
            warn_unenforced: false
      - name: city
        data_type: varchar
      - name: country
        data_type: varchar
      - name: longitude
        data_type: float
      - name: latitude
        data_type: float